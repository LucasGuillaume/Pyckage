import logging
import os

def [APPFUNCTION_1](loglevel, progress_bar):
    logging.debug('[DEBUG] Welcome in [NAME]. Now in [APPMODULE1].[APPFUNCTION]')
    logging.debug('[DEBUG] loglevel : {}, progress_bar : {}'.format(loglevel,progress_bar))
    return
